export enum RoundStatusEnum {
  PENDING = "pending",
  RUNNING = "running",
  ENDED = "ended",
}

export enum RoundPhaseEnum {
  IDLE = "idle",
  BETTING = "betting",
  RUNNING = "running",
  ENDED = "ended",
  PREPARING = "preparing",
  ERROR = "error",
}
